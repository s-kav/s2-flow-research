"""Solve Goldberg S2-flow for k=5,7,...,41 and verify by expanding to full graph."""
import numpy as np
from scipy.optimize import least_squares
import networkx as nx

def rot_z(a):
    c, s = np.cos(a), np.sin(a)
    return np.array([[c,-s,0],[s,c,0],[0,0,1]])

def goldberg_graph(k):
    G = nx.Graph()
    for t in range(k):
        for a, b in [(1,2),(1,7),(2,8),(3,4),(3,8),(4,7),(5,6),(6,7),(6,8)]:
            G.add_edge((t,a),(t,b))
    for t in range(k):
        G.add_edge((t,2),((t+1)%k,1))
        G.add_edge((t,4),((t+1)%k,3))
        G.add_edge((t,5),((t+1)%k,5))
    return G

internal_edges = [(1,2),(1,7),(2,8),(3,4),(3,8),(4,7),(5,6),(6,7),(6,8)]
inter_block_edges = [(2,1),(4,3),(5,5)]

def solve_goldberg(k, n_trials=300):
    n_orb = 12
    vinc = {j: [] for j in range(1, 9)}
    for oi, (a, b) in enumerate(internal_edges):
        vinc[a].append((oi, 0, +1))
        vinc[b].append((oi, 0, -1))
    for oi_off, (j1, j2) in enumerate(inter_block_edges):
        oi = 9 + oi_off
        vinc[j1].append((oi, 0, +1))
        vinc[j2].append((oi, k-1, -1))
    
    u = np.pi / k
    all_shifts = set()
    for j in range(1, 9):
        for _, s, _ in vinc[j]:
            all_shifts.add(s)
    Rp = {s: np.linalg.matrix_power(rot_z(2*u), s) for s in all_shifts}
    
    def residual(x):
        T = x.reshape(n_orb, 3)
        res = np.empty(36)
        idx = 0
        for j in range(1, 9):
            sv = np.zeros(3)
            for oi, sh, sgn in vinc[j]:
                sv += sgn * (Rp[sh] @ T[oi])
            res[idx:idx+3] = sv
            idx += 3
        for oi in range(n_orb):
            res[idx] = T[oi] @ T[oi] - 1.0
            idx += 1
        return res
    
    rng = np.random.default_rng(42 + k)
    best, xbest = np.inf, None
    for trial in range(n_trials):
        x0 = rng.normal(size=36)
        try:
            sol = least_squares(residual, x0, method='lm',
                                xtol=1e-15, ftol=1e-15, max_nfev=2000)
            r = float(np.max(np.abs(sol.fun)))
            if r < best:
                best, xbest = r, sol.x.copy()
            if best < 1e-13:
                break
        except:
            pass
    return best, xbest

def expand_and_verify(k, x36):
    """Expand templates to full G_k flow and verify Kirchhoff + unit norms."""
    G = goldberg_graph(k)
    theta = 2*np.pi/k
    T = x36.reshape(12, 3)
    Rp = {s: np.linalg.matrix_power(rot_z(theta), s) for s in range(k)}
    
    # assign flow to every oriented edge
    flow = {}
    for oi, (a, b) in enumerate(internal_edges):
        for s in range(k):
            src = ((0+s)%k, a)
            dst = ((0+s)%k, b)
            flow[(src, dst)] = Rp[s] @ T[oi]
    for oi_off, (j1, j2) in enumerate(inter_block_edges):
        oi = 9 + oi_off
        for s in range(k):
            src = ((0+s)%k, j1)
            dst = ((1+s)%k, j2)
            flow[(src, dst)] = Rp[s] @ T[oi]
    
    # Kirchhoff at every vertex
    max_kirch = 0.0
    for node in G.nodes():
        sv = np.zeros(3)
        for nbr in G.neighbors(node):
            if (node, nbr) in flow:
                sv += flow[(node, nbr)]
            elif (nbr, node) in flow:
                sv -= flow[(nbr, node)]
            else:
                print(f"  MISSING edge {node}-{nbr}")
                return None, None, None
        max_kirch = max(max_kirch, np.max(np.abs(sv)))
    
    # unit norms
    max_norm_dev = 0.0
    for vec in flow.values():
        max_norm_dev = max(max_norm_dev, abs(np.linalg.norm(vec) - 1.0))
    
    cubic = all(d == 3 for _, d in G.degree())
    return max_kirch, max_norm_dev, cubic

print(f"{'k':>5} {'residual':>12} {'kirch':>12} {'norm_dev':>12} {'cubic':>6}")
for k in (5, 7, 9, 11, 15, 21, 41, 101):
    r, x = solve_goldberg(k)
    if r < 1e-10 and x is not None:
        kirch, norm_dev, cubic = expand_and_verify(k, x)
        print(f"{k:5d} {r:12.2e} {kirch:12.2e} {norm_dev:12.2e} {str(cubic):>6}")
    else:
        print(f"{k:5d} {r:12.2e} {'FAILED':>12}")

The command exceeded the execution time limit. Try breaking it into smaller steps or adding a timeout to the command itself.

Solve Goldberg S2-flows with warm-starting from the previous solution

"""Solve Goldberg for a few k quickly: reduce trials, use warm-start from G_5 solution."""
import numpy as np
from scipy.optimize import least_squares

def rot_z(a):
    c, s = np.cos(a), np.sin(a)
    return np.array([[c,-s,0],[s,c,0],[0,0,1]])

internal_edges = [(1,2),(1,7),(2,8),(3,4),(3,8),(4,7),(5,6),(6,7),(6,8)]
inter_block_edges = [(2,1),(4,3),(5,5)]

def make_residual(k):
    vinc = {j: [] for j in range(1, 9)}
    for oi, (a, b) in enumerate(internal_edges):
        vinc[a].append((oi, 0, +1))
        vinc[b].append((oi, 0, -1))
    for oi_off, (j1, j2) in enumerate(inter_block_edges):
        oi = 9 + oi_off
        vinc[j1].append((oi, 0, +1))
        vinc[j2].append((oi, k-1, -1))
    u = np.pi / k
    shifts = set()
    for j in range(1, 9):
        for _, s, _ in vinc[j]: shifts.add(s)
    Rp = {s: np.linalg.matrix_power(rot_z(2*u), s) for s in shifts}
    
    def residual(x):
        T = x.reshape(12, 3)
        res = np.empty(36)
        idx = 0
        for j in range(1, 9):
            sv = np.zeros(3)
            for oi, sh, sgn in vinc[j]:
                sv += sgn * (Rp[sh] @ T[oi])
            res[idx:idx+3] = sv
            idx += 3
        for oi in range(12):
            res[idx] = T[oi] @ T[oi] - 1.0
            idx += 1
        return res
    return residual

def solve(k, x0=None, n_trials=100):
    F = make_residual(k)
    rng = np.random.default_rng(42 + k)
    best, xbest = np.inf, None
    starts = [x0] if x0 is not None else []
    starts += [rng.normal(size=36) for _ in range(n_trials)]
    for s in starts:
        try:
            sol = least_squares(F, s, method='lm', xtol=1e-15, ftol=1e-15, max_nfev=2000)
            r = float(np.max(np.abs(sol.fun)))
            if r < best:
                best, xbest = r, sol.x.copy()
            if best < 1e-13:
                return best, xbest
        except: pass
    return best, xbest

prev_x = None
for k in (5, 7, 9, 11, 15, 21, 41, 101, 1001):
    r, x = solve(k, x0=prev_x, n_trials=80)
    status = "OK" if r < 1e-10 else "FAIL"
    print(f"G_{k:5d}: residual = {r:.2e}  [{status}]")
    if r < 1e-10:
        prev_x = x
PYEOF
Output

G_    5: residual = 1.11e-16  [OK]
G_    7: residual = 6.53e-02  [FAIL]
G_5 works but G_7 fails with warm-starting. The template from G_5 isn't a good starting point for G_7 because the system's structure changes with k (different R matrix). Let me try harder with more random trials specifically for G_7, and also understand the structure better.

Let me check: for G_5 the system has 36 equations and 36 unknowns. The Jacobian should be square and the system should be well-posed near a solution. Let me check the condition number for G_5 and see if the solution is isolated.
I'm wondering if the 36×36 system has hidden gauge freedom like the flower snark case did with rotation about the z-axis. The Kirchhoff equations might have dependencies that make the system effectively underdetermined in some directions, which would explain why finding solutions is so difficult.

Let me fix this by adding a gauge condition—setting the y-component of the first template to zero—to pin down the solution uniquely. For G_7, I should also run many more random initializations to improve my chances of finding a valid configuration.

"""Debug: check Jacobian rank at the G_5 solution, add gauge, solve G_7."""
import numpy as np
from scipy.optimize import least_squares

def rot_z(a):
    c, s = np.cos(a), np.sin(a)
    return np.array([[c,-s,0],[s,c,0],[0,0,1]])

internal_edges = [(1,2),(1,7),(2,8),(3,4),(3,8),(4,7),(5,6),(6,7),(6,8)]
inter_block_edges = [(2,1),(4,3),(5,5)]

def make_system(k, add_gauge=False):
    vinc = {j: [] for j in range(1, 9)}
    for oi, (a, b) in enumerate(internal_edges):
        vinc[a].append((oi, 0, +1)); vinc[b].append((oi, 0, -1))
    for oi_off, (j1, j2) in enumerate(inter_block_edges):
        oi = 9 + oi_off
        vinc[j1].append((oi, 0, +1)); vinc[j2].append((oi, k-1, -1))
    u = np.pi / k
    shifts = set()
    for j in range(1,9):
        for _,s,_ in vinc[j]: shifts.add(s)
    Rp = {s: np.linalg.matrix_power(rot_z(2*u), s) for s in shifts}
    n_eq = 36 + (1 if add_gauge else 0)
    def residual(x):
        T = x.reshape(12, 3)
        res = np.empty(n_eq)
        idx = 0
        for j in range(1, 9):
            sv = np.zeros(3)
            for oi, sh, sgn in vinc[j]:
                sv += sgn * (Rp[sh] @ T[oi])
            res[idx:idx+3] = sv; idx += 3
        for oi in range(12):
            res[idx] = T[oi] @ T[oi] - 1.0; idx += 1
        if add_gauge:
            res[idx] = T[0][1]  # first template y-component = 0
        return res
    return residual

# Check Jacobian rank for G_5
F5 = make_system(5)
from scipy.optimize import approx_fprime
r5, x5 = np.inf, None
rng = np.random.default_rng(42+5)
for _ in range(200):
    sol = least_squares(F5, rng.normal(size=36), method='lm', xtol=1e-15, ftol=1e-15, max_nfev=2000)
    r = float(np.max(np.abs(sol.fun)))
    if r < r5: r5, x5 = r, sol.x.copy()
    if r5 < 1e-13: break
print(f"G_5 residual: {r5:.2e}")

J = np.array([approx_fprime(x5, lambda x: F5(x)[i:i+1], 1e-7).flatten() for i in range(36)])
sv = np.linalg.svd(J, compute_uv=False)
rank = np.sum(sv > 1e-10)
print(f"Jacobian at solution: shape {J.shape}, rank {rank}, smallest sv: {sv[-3:]}")
print(f"  → system has {36 - rank} gauge freedoms")

# Solve with gauge for G_5..G_41
F5g = make_system(5, add_gauge=True)
for _ in range(200):
    sol = least_squares(F5g, rng.normal(size=36), method='lm', xtol=1e-15, ftol=1e-15, max_nfev=2000)
    r = float(np.max(np.abs(sol.fun)))
    if r < 1e-13:
        x5g = sol.x.copy(); break

print(f"G_5 with gauge: {r:.2e}")
prev = x5g if r < 1e-10 else None

for k in (7, 9, 11, 15, 21, 41, 101):
    Fg = make_system(k, add_gauge=True)
    best, xbest = np.inf, None
    starts = ([prev] if prev is not None else []) + [rng.normal(size=36) for _ in range(400)]
    for s in starts:
        try:
            sol = least_squares(Fg, s, method='lm', xtol=1e-15, ftol=1e-15, max_nfev=2000)
            r = float(np.max(np.abs(sol.fun)))
            if r < best: best, xbest = r, sol.x.copy()
            if best < 1e-13: break
        except: pass
    print(f"G_{k:5d}: residual = {best:.2e}  [{'OK' if best<1e-10 else 'FAIL'}]")
    if best < 1e-10: prev = xbest
PYEOF
Output

G_5 residual: 1.11e-16
Jacobian at solution: shape (36, 36), rank 35, smallest sv: [3.13241415e-01 1.70263024e-01 1.24700165e-16]
  → system has 1 gauge freedoms
G_5 with gauge: 2.22e-16



продолжай



Claude is AI and can make mistakes. Please double-check responses.



