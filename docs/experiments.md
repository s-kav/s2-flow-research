# Experiments

Each run records graph diagnostics, solver cost, iteration count, conservation residual, unit-norm residual, Gram eigenvalues, numerical rank, and BQ residual. JSON is human-readable; NPZ contains the candidate matrices.
