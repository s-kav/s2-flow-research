# Algorithms

The default solver parameterizes every conservation-preserving flow as `X=ZY`, where columns of `Z` form an orthonormal basis of `ker(B)`. Nonlinear least squares then minimizes row-wise squared-norm residuals.
