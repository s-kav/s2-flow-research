# Theory

For an oriented graph with incidence matrix `B`, an S²-flow is an edge matrix `X ∈ R^{m×3}` satisfying `BX=0` and `diag(XXᵀ)=1`. Equivalently, `Q=XXᵀ` is PSD, has rank at most three, unit diagonal, and satisfies `BQ=0`.
